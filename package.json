{
  "name": "bst-operations-platform",
  "version": "0.1.0",
  "private": true,
  "scripts": { "typecheck": "node --check app.js && node --check google-adapter.js && node --check build.mjs", "build": "node --check app.js && node build.mjs", "test": "node --test tests/*.test.js" }
}
